# bbabdul

Abdul's portfolio — built with Next.js (App Router) + TypeScript.

## Dev

```bash
npm install
npm run dev
```

Open http://localhost:3000.

## Deploy

Push to GitHub and import on Vercel — zero config.
